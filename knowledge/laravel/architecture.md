# Laravel Architecture

Controller → Action → Service → Model

Actions coordinate a single use case.
Services encapsulate reusable business rules.
Repositories are used rarely.
Favor composition over inheritance.
